// script.js (File ini saat ini tidak wajib karena Bootstrap sudah menangani carousel)

document.addEventListener('DOMContentLoaded', function() {
    // Inisialisasi Carousel secara manual (optional di Bootstrap 5)
    // const bannerCarousel = document.getElementById('bannerCarousel');
    // if (bannerCarousel) {
    //     new bootstrap.Carousel(bannerCarousel, {
    //         interval: 5000, // Otomatis bergeser setiap 5 detik
    //         wrap: true // Kembali ke awal setelah slide terakhir
    //     });
    // }

    // Contoh: Log sederhana saat halaman dimuat
    console.log("Portal UMKM Malang: Landing Page siap!");
});
